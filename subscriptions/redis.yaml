kind: Subscription
metadata:
  name: redis-operator 
  namespace: operators
spec:
  package: redis-operator 
  channel: stable
  config:
    resources:
      requests:
        memory: "64Mi"
        cpu: "250m"
      limits:
        memory: "128Mi"
        cpu: "500m"